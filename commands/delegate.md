---
description: Route a task to the right free AI agent (mimo, opencode, gemini, codex). Claude Pro stays as director.
argument-hint: "[task description]"
---

Invoke the `delegate` skill to route the following task:

$ARGUMENTS

Classify the task type (Code / Research / Content / Image / Business / Document), pick the right agent from the routing table, output the exact command to run, and tell me your role as orchestrator after delegating.
